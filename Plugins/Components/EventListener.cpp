// local project headers
// ----------------------
#include "EventListener.h"

// standard C/C++ headers
// ----------------------

void EventListener::listen()
{
//    for (int event = 0; event != Event::All; event++)
//    {
//        Event currentEvent = static_cast<Event>(event);
//        m_activeEvents.insert(currentEvent);
//    }
}

//bool EventListener::isEventTriggered(RequestType event)
//{
//    auto it = m_activeEvents.find(event);
//    if (it == m_activeEvents.end())
//    {
//        return false;
//    }

//    return true;
//}
